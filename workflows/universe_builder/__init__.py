"""universe-builder surface — franchise/era/tone/canon-status authoring.

Owns the workshop steps that pin down the universe wrapper around a project:
project_scope (Step 0) and franchise/era/tone (Step 1). Produces the
``meta`` block of ``concept_seed.json`` plus the standalone
``franchise_meta.json`` artifact.
"""
