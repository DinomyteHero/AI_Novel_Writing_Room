"""scene-card-authoring surface — per-scene cards (Step 8).

Wraps ``src/planning/scene_card_generator.SceneCardGenerator`` and the
shape-translator at ``workflows/_shared/scene_card_translator.py``. SKILL.md
ships as a P2 deferral stub in Phase 4; api.py + legacy_seed importer are P1.
"""
