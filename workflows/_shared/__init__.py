"""Shared helpers used by every workflow surface.

Hosts the seed-mutation transforms (``seed_transforms``) and the scene-card
shape translator (``scene_card_translator``) that previously lived under
``src/concept_workshop/``. Both keep back-compat shims at their original
import paths during the Phase 4 migration window.
"""
