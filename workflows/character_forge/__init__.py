"""character-forge surface — ensemble_cast authoring (Step 4).

Owns three-dimensional character cards, weiland_arc plus arc_phase_map,
and arc-coverage validation against the chapter target. Produces the
``ensemble_cast`` slice of ``concept_seed.json``.
"""
